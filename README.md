***How to run the Dockerized app***

`docker-compose up -d`
*-d : for Detached mode*


**To stop the containers**

    docker-compose stop
**To remove and stop the containers**

    docker-compose down
